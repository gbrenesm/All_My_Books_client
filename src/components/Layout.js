import React from 'react'

function Layout({children}) {
  return (
    <div>
      <nav>
        <ul>
          
        </ul>
      </nav>
      <main>
        {children}
      </main>
    </div>
  )
}

export default Layout
